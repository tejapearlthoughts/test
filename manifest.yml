display_information:
  name: webhook
features:
  bot_user:
    display_name: webhook
    always_online: false
oauth_config:
  scopes:
    bot:
      - incoming-webhook
      - files:write
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
